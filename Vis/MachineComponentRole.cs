namespace CNCSS.Vis
{
    /// <summary>Target machine node role for an imported mesh component.</summary>
    public enum MachineComponentRole
    {
        Unassigned = 0,
        Spindle,
        Base,
        Table,
        Other
    }
}
